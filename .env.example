# Copy to .env next to docker-compose.yml and fill in. Never commit .env.
NOTION_API_KEY=ntn_...
# Generate with: openssl rand -hex 32
MCP_API_KEY=
# Optional overrides (defaults live in application.yml)
#NOTION_PROJECTS_DS_ID=
#NOTION_ARCHIVES_DS_ID=
#NOTION_TEMPLATE_ID=
